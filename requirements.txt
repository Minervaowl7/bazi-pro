# bazi-pro 依赖
# 完整可选依赖定义见 pyproject.toml [project.optional-dependencies]
# 安装方式: pip install bazi-pro[hybrid] / [report] / [pdf] / [server] / [tui] / [all]
#
# 核心依赖
jieba>=0.42.1          # 中文分词（BM25 检索）

# 报告生成（可选）
# markdown>=3.5        # Markdown→HTML 转换（可选，stdlib 有 fallback）
# weasyprint>=60       # HTML→PDF 转换（可选，需系统 GTK+ 依赖）
# pdfkit>=1.0          # HTML→PDF 转换（可选，需 wkhtmltopdf）

# 向量检索（可选，Hybrid Search 模式）
# sentence-transformers>=2.2  # embedding 生成
# faiss-cpu>=1.7            # 向量索引
# numpy>=1.24                # 数值计算
